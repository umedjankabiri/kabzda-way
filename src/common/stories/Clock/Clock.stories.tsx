import {Clock} from "common/components/Clock/Clock.tsx";

export default {
    title: "Clock stories",
    component: Clock
}

export const ClockDemonstration = () => {
    return (
        <Clock />
    )
}
